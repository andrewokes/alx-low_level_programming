#include "main.h"

/**
 * _abs - Compute the absolute valve
 * of a integer
 *
 * @n: input number as the integer
 *
 * Return: absolute value
 */

int _abs(int n)

{
	if (n < 0)
	{
	return (-n);
	}
	else
	{
	return (n);
	}
}
